export const brightness = (brightness)=>({
  type: 'BRIGHTNESS',
  payload: brightness
});

export const contrast = (contrast)=>({
  type: 'CONTRAST',
  payload: contrast
})

export const saturation = (saturation)=>({
  type: 'SATURATION',
  payload: saturation
})

export const hue = (hue)=>({
  type: 'HUE',
  payload: hue
})

export const sepia = (sepia)=>({
  type: 'SEPIA',
  payload: sepia
});

export const blur = (blur)=>({
  type: 'BLUR',
  payload: blur
})

export const noise = (noise)=>({
  type: 'NOISE',
  payload: noise
})

export const pixelate = (pixelate)=>({
  type: 'PIXELATE',
  payload: pixelate
})

export const mask = (mask)=>({
  type: 'MASK',
  payload: mask
})

export const grayScale = (grayScale)=>({
  type: 'GRAYSCALE',
  payload: grayScale
})


export const invert = (invert)=>({
  type: 'INVERT',
  payload: invert
})

export const kaleidoscope = (kaleidoscope)=>({
  type: 'KALEIDOSCOPE',
  payload: kaleidoscope
})
